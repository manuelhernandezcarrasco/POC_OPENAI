python3 -m venv venv
source venv/bin/activate
pip install python-dotenv

python3 -m modular_impl.scripts.main_script
python3 -m modular_impl.scripts.run_quantity_tests <rubro>
python3 -m modular_impl.scripts.run_quantity_tests <rubro> --caso <caso>
python3 -m modular_impl.scripts.run_quantity_tests_batch <rubro> --caso <caso> --cv_per_request n
